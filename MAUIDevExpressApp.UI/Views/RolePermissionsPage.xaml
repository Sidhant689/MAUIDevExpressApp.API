<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MAUIDevExpressApp.UI.Views.RolePermissionsPage"
             Title="{Binding Title}">

    <Grid RowDefinitions="Auto,*,Auto" Padding="20">
        <Label Text="{Binding Role.Name, StringFormat='Permissions for {0}'}"
               FontSize="Large"
               FontAttributes="Bold"/>

        <CollectionView Grid.Row="1" 
                       ItemsSource="{Binding AvailablePermissions}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <VerticalStackLayout>
                            <Label Text="{Binding Name}"
                                   FontAttributes="Bold"/>
                            <Label Text="{Binding Description}"
                                   FontSize="Small"/>
                            <Label Text="{Binding Module.Name}"
                                   FontSize="Micro"/>
                        </VerticalStackLayout>

                        <CheckBox Grid.Column="1"
                                 IsChecked="{Binding IsSelected}"
                                 VerticalOptions="Center"/>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Grid.Row="2"
                Text="Save Permissions"
                Command="{Binding SavePermissionsCommand}"
                BackgroundColor="Green"
                TextColor="White"/>
    </Grid>
</ContentPage>